1. Через командную строку сохранить все print'ы в .txt
python script1.py > saveit.txt
2. Переипорт скрипта ранее загруженного
    from imp import reload
    reload(script1)
3. dir(script1) получние всех имен модуля
4. Запуск скрипта из командной строки без явного импортирования (и последующего reload)
Одко перезаписывает перменные (если они совпадают) в коммандной строке
    exec(open('scripot1.py').read())